class BlockIndex:

    def __init__(self, level, startPtr, endPtr, num, rangeList, nums, rts,
                 tics, basePeakIntensities, basePeakMzs, mzs, tags, ints, mobilities, cvList, features):
        self.level = level
        self.startPtr = startPtr
        self.endPtr = endPtr
        self.num = num
        self.rangeList = rangeList
        self.nums = nums
        self.rts = rts
        self.tics = tics
        self.basePeakIntensities = basePeakIntensities
        self.basePeakMzs = basePeakMzs
        self.mzs = mzs
        self.tags = tags
        self.ints = ints
        self.mobilities = mobilities
        self.cvList = cvList
        self.features = features



